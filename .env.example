# Local PostgreSQL only. Copy to `.env` and adjust if your local user/db differ.
# Do not commit real secrets. Production hosting is still an open question.
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/ai_business_os"
